# 因子回测框架 v1 使用手册

## 1. 框架定位

本框架用于验证日频因子的截面排序能力，核心问题是：因子值高低排序是否能在未来 open-to-open 收益上形成稳定差异。它不是传统调仓组合回测框架。

因子标准输入为宽表：

```text
index = trade_date
columns = symbol
values = factor value
```

`factor_df.loc[t]` 表示 `t` 日收盘后生成、下一交易日开盘前可用于交易的因子值。未来收益按：

```text
future_return_h[t] = open_price[t+h+1] / open_price[t+1] - 1
```

这里的 `t+1`、`t+h+1` 都是交易日序列上的偏移，不是自然日偏移。

## 2. 安装方式

在服务器上建议安装为 editable package：

```bash
cd /app/workspace/zhangyuan/Factor_Backtest
/app/workspace/zhangyuan/.venv/bin/pip install -e .
```

安装后，调用脚本可以放在：

```text
/app/workspace/zhangyuan/Factor_Backtest_Result/factor_dm_20d/run_factor_dm_20d.py
```

## 3. 常用路径

```python
project_dir = "/app/workspace/zhangyuan/Factor_Backtest"
data_root = "/data/zhangyuan"
pool_dir = "/data/zhangyuan/pool"
output_root = "/data/zhangyuan/Factor_Backtest_Result"
```

默认因子文件发现规则：

```text
/data/zhangyuan/factor_dm_20d/factor_dm_20d.h5
/data/zhangyuan/factor_dm_20d/factor_dm_20d.csv
```

## 4. BacktestConfig 参数

| 参数 | 默认值 | 作用 |
| --- | --- | --- |
| `framework_version` | `"v1"` | 框架版本标记，写入 `run_meta.json` |
| `output_root` | `/data/zhangyuan/Factor_Backtest_Result` | 回测结果根目录 |
| `factor_name` | `None` | 输出目录使用的因子名 |
| `selected_pools` | `["all"]` | 股票池列表，`all` 表示全市场 |
| `horizons` | `[1, 5, 10, 20]` | open-to-open 未来收益周期 |
| `horizon_colors` | 柔和蓝橙绿红 | 图表中 1D/5D/10D/20D 颜色 |
| `min_listed_days` | `120` | 新股/次新股过滤阈值 |
| `tradability_filter` | `True` | 是否执行涨跌停、ST、停牌、新股过滤 |
| `min_ic_stocks` | `30` | 每日 IC 最小有效股票数 |
| `min_group_stocks` | `10` | 每日分组收益最小有效股票数 |
| `analysis_windows` | `[120, 250, 750]` | 预留分析窗口参数 |
| `group_return_windows` | `{"6m":120,"1y":250,"3y":750,"5y":1250}` | 分层收益统计窗口 |
| `enabled_sections` | `"all"` | 输出模块列表，或 `"all"` |
| `output_layout` | `"latest_runs"` | 输出结构，默认同时保留 `runs` 和 `latest` |
| `render_plots` | `True` | 是否在回测时直接画图 |
| `verbose` | `True` | 是否打印轻量进度日志 |

## 5. 股票池

默认：

```python
selected_pools = ["all"]
```

可选指数池：

```text
hs300_pool
zz500_pool
zz1000_pool
gz1000_pool
gz2000_pool
gzMidsmallcap_pool
miMicrocap_pool
```

每个 pool 独立计算 IC、分组收益、多空收益、质量指标和图表。

## 6. 完整回测

```python
from factor_backtest import BacktestConfig, run_factor_backtest

cfg = BacktestConfig(
    factor_name="factor_dm_20d",
    selected_pools=["all"],
    horizons=[1, 5, 10, 20],
    tradability_filter=True,
)

result = run_factor_backtest(
    factor_df=factor_df,
    market_data=market_data,
    config=cfg,
)

print(result.run_dir)
print(result.latest_dir)
```

默认输出结构：

```text
/data/zhangyuan/Factor_Backtest_Result/factor_dm_20d/
  latest/
  runs/
    20260520_093000_000000/
```

下游读取通常使用 `latest/`，历史追溯使用 `runs/<run_time>/`。

## 7. 最精简输出

用于批量因子筛选或后续模型训练：

```python
from factor_backtest import run_factor_backtest_minimal

summary = run_factor_backtest_minimal(
    factor_df=factor_df,
    market_data=market_data,
    config=cfg,
)
```

输出为 DataFrame，每个因子和 pool 一行，包含：

```text
ic_mean_1d, icir_1d, ic_t_stat_1d
long_short_mean_1d, long_short_sharpe_1d, long_short_t_stat_1d
coverage_mean, valid_factor_count_mean
```

各 horizon 会生成对应列。

## 8. 只跑数据和重新渲染

只计算数据和统计表、不画图：

```python
from factor_backtest import run_factor_backtest_data

result = run_factor_backtest_data(
    factor_df=factor_df,
    market_data=market_data,
    config=cfg,
)
```

之后基于已有结果重新画图并生成 HTML：

```python
from factor_backtest import render_factor_backtest_report

render_factor_backtest_report(result.run_dir)
```

如果希望默认读取入口 `latest/` 也生成图表，可以改为：

```python
render_factor_backtest_report(result.latest_dir)
```

## 9. 读取已有结果

```python
from factor_backtest import load_backtest_result

result = load_backtest_result(
    factor_name="factor_dm_20d",
    run="latest",
)

ic_stats = result.read_table("all", "ic_stats")
performance = result.read_table("all", "performance_metrics")
plot_path = result.plot_path("all", "cumulative_ic.png")
```

如需重新生成 HTML 报告：

```python
from factor_backtest import render_factor_backtest_report

render_factor_backtest_report(result.run_dir)
```

## 10. 输出说明

每个 pool 下：

```text
artifacts/  # 中间日频结果
tables/     # 统计表和模块输出表
plots/      # PNG 图表
```

`report.html` 是单次回测总入口，会嵌入关键图表和关键统计表。

## 11. 主要指标口径

RankIC：每日截面因子 rank 与未来收益 rank 的 Spearman 相关。因子日 `t` 对应的收益是下一交易日开盘买入、持有 `horizon` 个交易日后开盘卖出。

累计 IC：每日 IC 的累计和。

分组收益：每天按因子从低到高分 10 组，组内股票等权，之后对时间求均值。

多空收益：`G10 - G1`。`long_short_curve.png` 使用日度多空收益的累计和，不做复利。

分组收益：同时输出全样本 10 组 × 多 horizon 平均收益合并图，以及 1D、5D、10D、20D 各自单独一张 10 组累计收益线图。线图会先把 h 日未来收益转换为日均等价收益 `(1 + group_return) ** (1 / h) - 1`，再按复利生成净值序列，避免把重叠的 h 日收益当成每日收益重复复利。G1 为因子值最低组，G10 为因子值最高组。

分层收益：按 `group_return_windows` 分别统计最近 6 个月、1 年、3 年、5 年的 10 组收益，每个窗口一张图，图内同时展示 1D、5D、10D、20D。
